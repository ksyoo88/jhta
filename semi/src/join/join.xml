<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE sqlMap PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"
"http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap namespace="JOIN">

	<insert id="signUp" parameterClass="join.JoinVO">
	insert into
	member
	(MEMBER_ID, EMAIL, PASSWORD, NAME, BIRTH, GENDER, ADDR,
	ZIPCODE, PHONE, REGDATE)
	values
	(#member_id#, #email#, #password#, #name#,
	#birth#, #gender#, #addr#, #zipcode#, #phone#, sysdate)
	</insert>

</sqlMap>
